name: Build ImmortalWrt 25.12.2 Netcore N30 Pro

on:
  workflow_dispatch:

env:
  IMMORTALWRT_TAG: v25.12.2
  STARVPN_IPK: packages/starvpn_6.1.0_aarch64_cortex-a53.ipk

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            clang \
            flex \
            bison \
            gawk \
            gettext \
            git \
            libncurses-dev \
            libssl-dev \
            python3 \
            rsync \
            unzip \
            zstd \
            file \
            binutils

      - name: Clone ImmortalWrt
        run: |
          git clone \
            --depth 1 \
            --branch "${IMMORTALWRT_TAG}" \
            https://github.com/immortalwrt/immortalwrt.git \
            immortalwrt

      - name: Verify N30 Pro target
        working-directory: immortalwrt
        run: |
          grep -R "netis_nx30v2" target/linux/mediatek/image

      - name: Prepare StarVPN package
        run: |
          set -e

          test -f "${STARVPN_IPK}"

          rm -rf /tmp/starvpn
          mkdir -p /tmp/starvpn

          cd /tmp/starvpn

          ar x "${GITHUB_WORKSPACE}/${STARVPN_IPK}"

          test -f data.tar.gz

          tar xzf data.tar.gz

          cd "${GITHUB_WORKSPACE}"

          mkdir -p immortalwrt/package/starvpn/starvpn/files

          if [ -d /tmp/starvpn/usr ]; then
            cp -a /tmp/starvpn/usr \
            immortalwrt/package/starvpn/starvpn/files/
          fi

          if [ -d /tmp/starvpn/etc ]; then
            cp -a /tmp/starvpn/etc \
            immortalwrt/package/starvpn/starvpn/files/
          fi

          find /tmp/starvpn -maxdepth 3 -type f

      - name: Create StarVPN Makefile
        run: |
          mkdir -p immortalwrt/package/starvpn/starvpn

          printf '%s\n' \
          'include $(TOPDIR)/rules.mk' \
          'PKG_NAME:=starvpn' \
          'PKG_VERSION:=6.1.0' \
          'PKG_RELEASE:=1' \
          'include $(INCLUDE_DIR)/package.mk' \
          '' \
          'define Package/starvpn' \
          ' SECTION:=net' \
          ' CATEGORY:=Network' \
          ' TITLE:=StarVPN' \
          ' DEPENDS:=+kmod-tun' \
          'endef' \
          '' \
          'define Package/starvpn/install' \
          ' $(INSTALL_DIR) $(1)/usr/bin' \
          ' cp -a ./files/usr/bin/* $(1)/usr/bin/' \
          ' $(INSTALL_DIR) $(1)/etc/init.d' \
          ' cp -a ./files/etc/init.d/starvpn $(1)/etc/init.d/' \
          'endef' \
          '' \
          '$(eval $(call BuildPackage,starvpn))' \
          > immortalwrt/package/starvpn/starvpn/Makefile

      - name: Configure feeds
        working-directory: immortalwrt
        run: |
          echo "src-link starvpn $(pwd)/package/starvpn" >> feeds.conf.default
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          ./scripts/feeds install starvpn

      - name: Configure firmware
        working-directory: immortalwrt
        run: |
          printf '%s\n' \
          'CONFIG_TARGET_mediatek=y' \
          'CONFIG_TARGET_mediatek_filogic=y' \
          'CONFIG_TARGET_DEVICE_mediatek_filogic_DEVICE_netis_nx30v2=y' \
          'CONFIG_PACKAGE_luci=y' \
          'CONFIG_PACKAGE_luci-ssl=y' \
          'CONFIG_PACKAGE_kmod-usb3=y' \
          'CONFIG_PACKAGE_kmod-usb-storage=y' \
          'CONFIG_PACKAGE_kmod-usb-storage-uas=y' \
          'CONFIG_PACKAGE_block-mount=y' \
          'CONFIG_PACKAGE_kmod-usb-net=y' \
          'CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y' \
          'CONFIG_PACKAGE_kmod-usb-net-cdc-ncm=y' \
          'CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=y' \
          'CONFIG_PACKAGE_kmod-usb-serial=y' \
          'CONFIG_PACKAGE_kmod-usb-serial-option=y' \
          'CONFIG_PACKAGE_kmod-usb-wdm=y' \
          'CONFIG_PACKAGE_usb-modeswitch=y' \
          'CONFIG_PACKAGE_uqmi=y' \
          'CONFIG_PACKAGE_umbim=y' \
          'CONFIG_PACKAGE_mwan3=y' \
          'CONFIG_PACKAGE_luci-app-mwan3=y' \
          'CONFIG_PACKAGE_samba4-server=y' \
          'CONFIG_PACKAGE_luci-app-samba4=y' \
          'CONFIG_PACKAGE_kmod-tun=y' \
          'CONFIG_PACKAGE_starvpn=y' \
          > .config

          make defconfig

      - name: Build
        working-directory: immortalwrt
        run: |
          make download -j$(nproc)
          make -j$(nproc) V=s

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-25.12.2-Netcore-N30-Pro
          path: immortalwrt/bin/targets/mediatek/filogic/
